---
import { ArrowRight } from 'lucide-react'
import type { Language } from '../../i18n/languages'
import { getTranslations } from '../../i18n/t'
import { formatLongDate, isoDate } from '../../lib/formatDate'
import { releaseSlug, type ReleaseEntry } from '../../lib/releases'

interface Props {
  entry: ReleaseEntry
  language: Language
}

const { entry, language } = Astro.props
const t = getTranslations(language, 'releases')
const { data } = entry
const href = `/${language}/releases/${releaseSlug(entry)}`
---

<a
  href={href}
  class="group block bg-gradient-to-br from-green-dark-900 to-green-middle-900 rounded-2xl lg:rounded-3xl p-6 lg:p-10 shadow-lg ring-1 ring-white/10 transition-all duration-300 hover:-translate-y-1 hover:shadow-xl"
>
  <div class="flex flex-wrap items-center gap-3 mb-4">
    <span
      class="inline-flex items-center bg-white/15 text-white px-4 py-1.5 rounded-full text-sm font-lato font-bold ring-1 ring-white/20"
      >v{data.version}</span
    >
    <span
      class="inline-flex items-center bg-green-light-900 text-white px-3 py-1 rounded-full text-xs font-bold uppercase tracking-wider"
      >{t('badge.current')}</span
    >
    <time datetime={isoDate(data.date)} class="text-white/60 text-sm font-medium">
      {formatLongDate(data.date, language)}
    </time>
  </div>

  <h2 class="font-lato font-bold text-2xl lg:text-4xl text-white mb-3 lg:mb-4 max-w-2xl">
    {data.title}
  </h2>

  {
    data.summary && (
      <p class="text-white/75 leading-relaxed text-base lg:text-lg max-w-2xl mb-6">
        {data.summary}
      </p>
    )
  }

  {
    data.highlights.length > 0 && (
      <div class="flex flex-wrap gap-2 mb-6 lg:mb-8">
        {data.highlights.map((highlight) => (
          <span class="bg-white/10 text-white/90 px-3 py-1.5 rounded-lg text-sm font-medium ring-1 ring-white/10">
            {highlight}
          </span>
        ))}
      </div>
    )
  }

  <div class="flex items-center gap-3 text-white/80 transition-colors group-hover:text-white">
    <span class="text-sm font-semibold">{t('featured.viewDetails')}</span>
    <span
      class="inline-flex items-center justify-center w-8 h-8 rounded-full bg-white/10 transition-all duration-300 group-hover:bg-white/20 group-hover:translate-x-1"
    >
      <ArrowRight className="w-4 h-4" aria-hidden="true" />
    </span>
  </div>
</a>
