---
import Arrow from '../icons/Arrow.astro'
import type { Language } from '../../i18n/languages'
import { getTranslations } from '../../i18n/t'
import { workflowSteps } from '../../data/workflowSteps'

interface Props {
  language: Language
}

const { language } = Astro.props
const t = getTranslations(language, 'home')

// The router Link carried hash="process"; as a plain anchor the fragment goes
// straight into the href.
const moreHref = `/${language}/project#process`
const lastIndex = workflowSteps.length - 1
---

<section
  class="max-w-208 mx-auto my-20 px-4 md:px-6 lg:my-28 lg:max-w-screen-lg xl:my-36 xl:max-w-screen-xl"
>
  <div class="flex items-end justify-between mb-8 lg:mb-12">
    <div>
      <div class="inline-block mb-4">
        <span class="text-xs font-semibold tracking-widest text-green-light-900 uppercase">
          {t('workflow.sectionLabel')}
        </span>
        <div class="h-0.5 w-12 bg-gradient-to-r from-green-light-900 to-transparent mt-1"></div>
      </div>

      <h2 class="font-lato font-bold text-2xl text-grey-900 lg:text-3xl xl:text-4xl">
        {t('workflow.title')}
      </h2>
    </div>

    <a
      href={moreHref}
      aria-label={t('workflow.moreLinkAriaLabel')}
      class="hidden lg:flex group items-center gap-x-2 font-semibold text-green-dark-900 hover:text-green-light-900 transition-colors duration-300 whitespace-nowrap"
    >
      {t('workflow.moreLink')}
      <Arrow class="w-4 h-4 transition-transform duration-300 group-hover:translate-x-1" />
    </a>
  </div>

  <div class="bg-green-light-100/40 rounded-2xl lg:rounded-3xl p-6 md:p-8 lg:p-10 xl:p-12">
    <div class="grid grid-cols-1 md:grid-cols-3 gap-8 md:gap-0">
      {
        workflowSteps.map((step, index) => {
          const Icon = step.icon

          return (
            <article
              class:list={[
                index < lastIndex && 'md:border-r md:border-green-dark-900/10 md:pr-8 lg:pr-10',
                index > 0 && 'md:pl-8 lg:pl-10',
                index > 0 && 'border-t border-green-dark-900/10 pt-8 md:border-t-0 md:pt-0',
              ]}
            >
              <Icon className="w-8 h-8 text-green-dark-900/40 mb-5 lg:w-10 lg:h-10" />

              <span class="text-xs font-semibold tracking-widest uppercase text-green-dark-900/60 block mb-2">
                {step.number} · {t(`workflow.steps.${step.id}.category`)}
              </span>

              <h3 class="font-lato font-bold text-xl text-grey-900 mb-3 lg:text-2xl">
                {t(`workflow.steps.${step.id}.title`)}
              </h3>

              <p class="text-grey-600 text-sm leading-relaxed">
                {t(`workflow.steps.${step.id}.description`)}
              </p>
            </article>
          )
        })
      }
    </div>
  </div>

  <div class="flex justify-center mt-8 lg:hidden">
    <a
      href={moreHref}
      aria-label={t('workflow.moreLinkAriaLabel')}
      class="group flex items-center gap-x-2 font-semibold text-green-dark-900 hover:text-green-light-900 transition-colors duration-300"
    >
      {t('workflow.moreLink')}
      <Arrow class="w-4 h-4 transition-transform duration-300 group-hover:translate-x-1" />
    </a>
  </div>
</section>
